# מדריך פסנתר — הנחיות עבודה בריפו הזה

## כלל ברזל: פריסה אחרי כל ביצוע

המשתמש אישר במפורש, כהוראה קבועה: **אחרי כל שינוי שהושלם ונבדק — למזג ל-`main` ולדחוף.**
לא להשאיר עבודה גמורה יושבת רק על ברנץ' הפיתוח.

הרצף בכל פעם:

```bash
# 1. פיתוח + בדיקה על ברנץ' העבודה
git add -A && git commit -m "..."
git push -u origin <branch>

# 2. מיזוג ופריסה
git checkout main
git merge --no-ff <branch> -m "Deploy: ..."
git push origin main
git checkout <branch>
```

GitHub Pages מוגדר על **`main` / שורש הריפו** (workflow `pages-build-deployment`),
כך שדחיפה ל-`main` היא הפריסה עצמה. הכתובת החיה:
`https://lironcon2015-prog.github.io/Workout-tracker-2.0/`

## לבמפ בכל פריסה

`sw.js` → `const VERSION = 'YYYY-MM-DD.N'` — המחרוזת הזו היא שם המטמון וגם מה
שמוצג למשתמש בפאנל תחת "גרסה ועדכונים". **בלי במפ, המשתמש לא יראה שהגרסה התחלפה.**

## מבנה

- שורש הריפו = אפליקציית הפסנתר (ES modules, בלי שלב בנייה, בלי תלויות).
- `legacy-workout/` = האפליקציה הקודמת בארכיון. לא לגעת.
- הסבר מלא על הארכיטקטורה, פורמט התווים ומגבלות iOS — ב-`README.md`.

## בדיקה לפני פריסה

אין מסגרת טסטים. הבדיקה היא הרצה אמיתית בדפדפן:

```bash
python3 -m http.server 8000
```
ואז Playwright מול `http://localhost:8000` — לוודא שאין שגיאות קונסולה,
שהיצירות נטענות, שהנגינה מתקדמת ושמצב "המתן לי" מתקדם תו-אחר-תו.
כרומיום נמצא ב-`/opt/pw-browsers/chromium-1194/chrome-linux/chrome`.

## שפה

הממשק, ההערות בקוד וההודעות למשתמש — בעברית. הודעות קומיט — עברית או אנגלית.
